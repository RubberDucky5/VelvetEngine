#include "sdlutil.h"
#include "mathutil.h"








// Lib makes a few assumtions
// RGBA8888 Pixel Format
// texture already locked

// void tline(void* pixels, int pitch, Vec2 p1, Vec2 p2, uint32_t color) {
    
// }

// void tpixel(void* pixels, int pitch, Vec2 p, uint32_t color) {
//     *(uint32_t*)((uint8_t*)pixels + p.y * (pitch/4) + p.x) = color;
// }